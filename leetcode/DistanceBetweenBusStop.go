package main

import "fmt"

func distanceBetweenBusStops(distace []int, start int, destination int) int {
	var distance int

	for i := 0; i < len(distace); i++ {

	}
}

func main() {
	fmt.Println(distanceBetweenBusStops([]int{1, 2, 3, 4}, 0, 1))
}
